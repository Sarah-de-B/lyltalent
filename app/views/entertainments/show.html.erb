<div id='container'>
  <div class="event-image-container">
    <% if @entertainment.event.photo.attached? %>
      <%= image_tag(@entertainment.event.photo.url, class: 'event-image') %>
    <% end %>
  </div>
  <h1><%= @entertainment.event.name %></h1>
  <h3><%= @entertainment.event.date %></h3>
  <br>
  <h3>Description de la soiree</h3>
  <br>
  <p><%= @entertainment.event.description %></p>
  <br>
  <h2>details de l'evenement</h2>
  <p> La soiree sera le <%= @entertainment.event.date %> et commence a
  <%= @entertainment.starts_at %> jusqu'a <%= @entertainment.ends_at %></p>
  <p>Public attendu d'environ <%= @entertainment.event.estimated_public_in_number %> personnes
  et l'age est d'environ <%= @entertainment.event.public_age %> ans</p>
  <p>Concernant l'evenement</p>
  <p>le Genre sera <%= @entertainment.event.musical_styles.join(", ") %> pour une duree de <%= @entertainment.event.duration %> minutes</p>
  <h2> Evenement <%= @entertainment.event.description %></h2>
  <p>Les Conditions</p>
  <p><%= @entertainment.planned_remuneration %></p>
  <% if @entertainment.planned_remuneration_in_euros.present? %>
    <p><%= @entertainment.planned_remuneration_in_euros  %> </p>
  <% end %>
  <p>Sont pris en charge par l'evenement (VHR): <%= @entertainment.included_services.join(', ') %> </p>
    <div class="btn-container">
      <%= link_to "Postuler", new_entertainment_entertainment_application_path(@entertainment), class: "bottom-middle-btn" %>
    </div>
  <h2>Ils participent a l'event</h2>
  <div class="user-image-container">
    <% @entertainment.entertainment_applications.each do |application| %>
      <%= image_tag(application.user.photos.first.url, class: 'user-image') %>
    <% end %>
   </div>
</div>
