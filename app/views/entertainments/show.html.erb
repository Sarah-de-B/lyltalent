<div id='container'>
  <div class="event-image-container">
    <% if @entertainment.event.photo.attached? %>
      <%= image_tag(@entertainment.event.photo.url, class: 'event-image') %>
    <% end %>
  </div>

  <div class="text-container">
    <h1 class="text-center text-bold"><%= @entertainment.event.name %></h1>
    <h3 class="text-center">📅 <%= @entertainment.event.date %></h3>
    <br>

    <h3 class="text-bold">Description de la soirée</h3>
    <br>
    <p><%= @entertainment.event.description %></p>
    <br>

    <h2 class="text-bold">Détails de l'événement</h2>
    <p>La soirée sera le <%= @entertainment.event.date %> et commence à
    <%= @entertainment.starts_at %> jusqu'à <%= @entertainment.ends_at %></p>

    <p>👥 Public attendu d'environ <%= @entertainment.event.estimated_public_in_number %> personnes
    et l'âge est d'environ <%= @entertainment.event.public_age %> ans</p>

    <p>Concernant l'événement :</p>
    <p>Le genre sera <%= @entertainment.event.musical_styles.join(", ") %> pour une durée de <%= @entertainment.event.duration %> minutes</p>

    <h2 class="text-bold">Événement <%= @entertainment.event.description %></h2>

    <p class="text-bold"> Les conditions :</p>
    <p><%= @entertainment.planned_remuneration %></p>

    <% if @entertainment.planned_remuneration_in_euros.present? %>
      <p><%= @entertainment.planned_remuneration_in_euros %></p>
    <% end %>

    <p>Sont pris en charge par l'événement (VHR) : <%= @entertainment.included_services.join(', ') %></p>

    <div class="btn-container">
      <%= link_to "Postuler", new_entertainment_entertainment_application_path(@entertainment), class: "bottom-middle-btn" %>
    </div>

    <div class="text-bold-center">
      <h2>Ils participent à l'event</h2>
    </div>
  </div>

  <div class="user-image-container">
    <% @entertainment.entertainment_applications.each do |application| %>
      <%= image_tag(application.user.photos.first.url, class: 'user-image') %>
    <% end %>
  </div>
</div>
